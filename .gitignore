# Secrets — never committed. Site/email defaults live in tools/atl/.env (committed).
.env.local
*.env.local

# Node
node_modules/
coverage/
tools/atl/dist/

# Release artifacts (scripts/package-release.ps1)
releases/
