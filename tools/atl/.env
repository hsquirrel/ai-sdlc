# atl — non-secret defaults. Committed.
# Secrets (ATL_TOKEN) go in .env.local, which is git-ignored.
# Precedence: real environment variables > .env.local > .env
ATL_SITE=kestra.atlassian.net
ATL_EMAIL=jeremy.harrell@kestrafinancial.com
